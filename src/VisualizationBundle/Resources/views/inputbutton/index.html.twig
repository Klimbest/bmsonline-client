{% extends 'base.html.twig' %}

{% block body %}
    <h1>InputButton list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for inputButton in inputButtons %}
            <tr>
                <td><a href="{{ path('inputbutton_show', { 'id': inputButton.id }) }}">{{ inputButton.id }}</a></td>
                <td>{{ inputButton.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('inputbutton_show', { 'id': inputButton.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('inputbutton_edit', { 'id': inputButton.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('inputbutton_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
