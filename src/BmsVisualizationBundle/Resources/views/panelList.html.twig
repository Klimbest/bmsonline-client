{% for panel in panels %}
    <div id="{{panel.id}}" class="panel-list text-center">
        <div class="row">
            <div class="col-md-12">
                {% if panel.type == "text" %}
                    <i class="fa fa-font fa-fw icon-type"></i> 
                {% elseif panel.type == "image" %}
                    <i class="fa fa-picture-o fa-fw icon-type"></i> 
                {% elseif panel.type == "variable" %}
                    <i class="fa fa-hdd-o fa-fw icon-type"></i> 
                {% endif %}
                
                <i class="fa fa-arrow-up fa-fw"></i>
                <i class="fa fa-arrow-down fa-fw"></i>
                <i class='fa fa-fw fa-clone fa-blue'></i>
                <i class='fa fa-fw fa-cogs fa-orange'></i>
                <i class='fa fa-fw fa-remove fa-red'></i>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <span class="label label-primary panel-list-{{panel.type}}" value="{{panel.zIndex}}">#{{panel.id}} - {{panel.name|length > 20 ? panel.name|slice(0, 20) ~ '...' : panel.name}} </span>
            </div>
        </div>
    </div>
{% endfor %}