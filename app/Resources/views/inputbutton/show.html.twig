{% extends 'base.html.twig' %}

{% block body %}
    <h1>InputButton</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inputButton.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ inputButton.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('inputbutton_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('inputbutton_edit', { 'id': inputButton.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
