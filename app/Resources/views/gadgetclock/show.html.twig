{% extends 'base.html.twig' %}

{% block body %}
    <h1>GadgetClock</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gadgetClock.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gadgetClock.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('gadgetclock_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('gadgetclock_edit', { 'id': gadgetClock.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
